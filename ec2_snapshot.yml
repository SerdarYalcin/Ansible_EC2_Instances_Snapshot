---
-
   hosts: localhost
   connection: local
   tasks:
           - name : INFRASTRUCTURE ADD SNAPSHOT
             ec2_snapshot:
                     instance_id: "{{ instanceid }}"
                     device_name: "{{ device }}"
                     region: eu-central-1
                     last_snapshot_min_age: 60

